#!/bin/bash
# Indicates whether or not you're in a screen session.
#
# Still considering how to present the information: if you're in screen or
# not is easy, but what to do with the detached screens count?  For the
# moment provide it as a count available for the user to use.

function _p_screen () {
    if [ $(which screen > /dev/null ; echo $?) -eq 0 ]
    then # screen is installed
        # Thanks to Jasper Boot for this one:
        export _p_detached_screens=$(screen -ls | command grep -c Detach )
        if [ "${TERM}" = "screen" ]
        then
            echo -n "scr"
        fi
    fi
}

